let word = 'nhg';
if (word === 'a' || word === 'e' || word === 'i' || word === 'o' || word === 'u' 
    || word === 'A' || word === 'E' || word === 'I' || word === 'O' || word === 'U') {
   document.write('${word} is a vowel')
}else{
    document.write('${word} is a consonant')
}